# Web framework for building the backend
Flask==2.0.1

# ORM for database interactions (e.g., managing power banks and rentals)
SQLAlchemy==1.4.22

# Integration of SQLAlchemy with Flask for easier database handling
Flask-SQLAlchemy==2.5.1

# PostgreSQL adapter for connecting to a PostgreSQL database
psycopg2-binary==2.9.1

# JWT authentication for securing endpoints (e.g., user login)
Flask-JWT-Extended==4.3.1

# Handling Cross-Origin Resource Sharing for frontend-backend communication
Flask-CORS==3.0.10
